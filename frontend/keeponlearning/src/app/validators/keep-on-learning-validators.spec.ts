import { KeepOnLearningValidators } from './keep-on-learning-validators';

describe('KeepOnLearningValidators', () => {
  it('should create an instance', () => {
    expect(new KeepOnLearningValidators()).toBeTruthy();
  });
});
